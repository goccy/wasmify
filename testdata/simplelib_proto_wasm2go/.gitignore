# Generated by `buf generate`. The protoc-gen-wasmify-go e2e test runs
# buf generate before invoking `go test`, so these files are produced
# on demand and do not live in the repository.
simplelib.go
/internal/
